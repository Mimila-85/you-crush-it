<div class="workout">
  <Span class="routine">{{name_routine}}</Span>
  {{#each array_of_exercises}}
  <div class="row">
    <div class="">{{name}}</div>
    <div class="">{{reps}}</div>
    <div class="">{{sets}}</div>
  </div>
  {{/each}}
</div>
</div>